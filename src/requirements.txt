# Python dependencies for PBMC clustering pipelines
# Tested with Python 3.10+
#
# Install:
#   pip install -r requirements.txt
#
# JAX must be installed separately (CPU-only build):
#   pip install "jax[cpu]"

scanpy==1.10.1
anndata
scvi-tools
leidenalg==0.10.2
igraph==0.11.4
scrublet==0.2.3
umap-learn==0.5.6
magic-impute
hdbscan
scikit-learn
seaborn
matplotlib
numpy
pandas
